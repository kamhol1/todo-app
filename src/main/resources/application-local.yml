spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: 'jdbc:h2:file:./db'
    username: 'sa'
    password: 'password'
  jpa:
    database-platform: 'org.hibernate.dialect.H2Dialect'
    hibernate:
      ddl-auto: validate
  h2:
    console:
      enabled: true
      path: '/console'
